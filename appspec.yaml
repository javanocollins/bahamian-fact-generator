version: 0.0
os: linux
files:
  - source: /dist
    destination: /var/www/html

hooks:
  BeforeInstall:
    - location: scripts/clean_build_directory.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/verify_deployment.sh
      timeout: 300
      runas: root
